services:
  - type: web
    name: spectra-qe
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: PORT
        value: "8000"
    healthCheckPath: /api/health
